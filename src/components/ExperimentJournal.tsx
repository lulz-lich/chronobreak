import { Trash2 } from "lucide-react";
import type { SavedExperiment, SimulationState } from "../types";
type Props={experiments:SavedExperiment[];onLoad:(state:SimulationState)=>void;onDelete:(id:string)=>void;onClear:()=>void};
export function ExperimentJournal({experiments,onLoad,onDelete,onClear}:Props){ return <section className="panel journalPanel"><div className="journalHeader"><div><p className="eyebrow">Experiment journal</p><h2>Saved states</h2></div>{experiments.length>0&&<button className="ghostButton" onClick={onClear}>Clear</button>}</div>{experiments.length===0?<p className="emptyState">No saved experiments yet. Save one before entropy eats your progress.</p>:<div className="journalList">{experiments.map((e)=><article key={e.id} className="journalItem"><button className="journalLoad" onClick={()=>onLoad(e.state)}><strong>{e.name}</strong><span>{new Date(e.createdAt).toLocaleString()} · {e.state.mode} · mass {e.state.mass.toFixed(1)}</span></button><button className="deleteButton" onClick={()=>onDelete(e.id)} aria-label="Delete experiment"><Trash2 size={16}/></button></article>)}</div>}</section>; }
