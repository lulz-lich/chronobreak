import { Activity, Gauge, Radio, Satellite, TimerReset, Waves } from "lucide-react";
import { formatNumber, percent } from "../lib/math";
import { metricSummary } from "../lib/relativity";
import type { SimulationState } from "../types";
type Props = { state: SimulationState };
function MetricCard({ icon, title, value, detail }: { icon: React.ReactNode; title: string; value: string; detail: string }) { return <article className="metricCard"><div className="metricIcon">{icon}</div><div><p>{title}</p><strong>{value}</strong><span>{detail}</span></div></article>; }
export function MetricsGrid({ state }: Props) { const m=metricSummary(state.mass,state.observerRadius); return <section className="metricsGrid"><MetricCard icon={<TimerReset size={22}/>} title="Time dilation" value={`${formatNumber(m.dilation,4)}×`} detail="Local clock rate compared to a distant observer."/><MetricCard icon={<Waves size={22}/>} title="Redshift" value={formatNumber(m.redshift,3)} detail="How much outgoing light is stretched."/><MetricCard icon={<Satellite size={22}/>} title="Escape velocity" value={`${percent(m.escapeVelocity)} c`} detail="Fraction of light speed needed to escape."/><MetricCard icon={<Activity size={22}/>} title="Orbit state" value={m.condition.toUpperCase()} detail="Qualitative stability near this radius."/><MetricCard icon={<Radio size={22}/>} title="Signal delay" value={`${formatNumber(m.delay,2)}×`} detail="Communication delay from near gravity well."/><MetricCard icon={<Gauge size={22}/>} title="Tidal stress" value={percent(m.tidal,2)} detail="Simplified stress index from gravity gradient."/></section>; }
